name: docs-deploy
on:
  push:
    branches:
      - root
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.x
      - run: mkdocs gh-deploy --force